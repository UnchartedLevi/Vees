import type { Post,ScheduledPost } from "../types";import{requireSupabase}from"../lib/supabaseClient";import{mapPost,mapScheduledPost}from"./mappers";
export async function getPosts(workspaceId:string){const{data,error}=await requireSupabase().from("posts").select("*").eq("workspace_id",workspaceId).eq("status","published").order("posted_at",{ascending:false});if(error)throw error;return(data??[]).map(mapPost)}
export async function getScheduledPosts(workspaceId:string){const{data,error}=await requireSupabase().from("posts").select("*").eq("workspace_id",workspaceId).neq("status","published").order("scheduled_at");if(error)throw error;return(data??[]).map(mapScheduledPost)}
export async function addManualPost(workspaceId:string,p:Post){const{data,error}=await requireSupabase().from("posts").insert({workspace_id:workspaceId,platform:p.platform,title:p.title,content_type:p.contentType,status:"published",content_goal:p.contentGoal,media_url:p.mediaUrl??null,external_url:p.externalUrl??null,external_post_id:p.externalPostId??null,duration_seconds:p.durationSeconds??null,retention_rate:p.retentionRate??null,posted_at:`${p.datePosted}T12:00:00Z`,likes:p.likes,comments:p.comments,shares:p.shares,saves:p.saves,reach:p.reach,impressions:p.impressions,campaign_id:p.campaignId||null,source:"manual",engagement_rate:p.reach?((p.likes+p.comments+p.shares+p.saves)/p.reach)*100:0}).select().single();if(error)throw error;return mapPost(data)}
export async function addScheduledPost(workspaceId:string,p:ScheduledPost){const{data,error}=await requireSupabase().from("posts").insert({workspace_id:workspaceId,platform:p.platform,title:p.title,caption:p.notes,content_type:p.contentType,status:p.status.toLowerCase().replace(" ","_"),content_goal:p.contentGoal,scheduled_at:`${p.scheduledDate}T12:00:00Z`,campaign_id:p.campaignId||null,source:"manual"}).select().single();if(error)throw error;return mapScheduledPost(data)}
export async function updatePostStatus(workspaceId:string,id:string,status:string){const normalized=status.toLowerCase().replace(/ /g,"_");const values=normalized==="published"?{status:normalized,posted_at:new Date().toISOString()}:{status:normalized};const{error}=await requireSupabase().from("posts").update(values).eq("workspace_id",workspaceId).eq("id",id);if(error)throw error}
export async function deletePost(workspaceId:string,id:string){const{error}=await requireSupabase().from("posts").delete().eq("workspace_id",workspaceId).eq("id",id);if(error)throw error}
